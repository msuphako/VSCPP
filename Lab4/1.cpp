#include <iostream>
using namespace std;
int main (){
    int a,b;
    cout << "Enter value a and b : ";
    cin >> a >>b ;
    if(a == b )cout << "a and b values are equal " << endl;
    if(a > b )cout << "a values > values b  equal " << endl;
    if(a < b )cout << "a values < values b equal " << endl;
    return 0;
}